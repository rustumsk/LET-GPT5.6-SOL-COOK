# The validation asset works without secrets.
# Add optional service credentials only in .env.local or encrypted provider storage.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
NEXT_PUBLIC_ANALYTICS_MODE=local

